name: Deploy Bot to VDS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to VDS
      uses: appleboy/ssh-action@v0.1.5
      with:
        host: 94.250.251.17
        username: root
        password: k.5002*s
        script: |
          cd /home/ubuntu
          git clone https://github.com/devilnevercry2212/telegram-bot-vds.git
          cd telegram-bot-vds
          apt update
          apt install python3 python3-pip python3-venv -y
          python3 -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt
          cp .env.example .env
          echo "TOKEN=8467184939:AAGsFjLQAHcMTfKsTAnbPTBC0YSlaQkGxpg" >> .env
          echo "CHAT_ID=-1003052947504" >> .env
          python3 bot.py
